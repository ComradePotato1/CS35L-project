{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "express": "npm start --prefix express",
    "react": "npm start --prefix react",
    "start": "concurrently \"npm run express\" \"npm run react\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
